import { Dialog } from '@mui/material';
import styled from 'styled-components';

export const StyledDialog = styled(Dialog).attrs({
  sx: { '& .MuiPaper-root': { width: '800px', height: '600px' } },
})``;
