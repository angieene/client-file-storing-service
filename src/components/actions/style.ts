import { SpeedDial } from '@mui/material';
import styled from 'styled-components';

export const StyledSpeedDial = styled(SpeedDial).attrs({
  sx: { position: 'absolute', right: 90 },
  direction: 'down',
})``;
